/**
 * Data Transfer Object barrel export.
 *
 * DTOs define the shape of data crossing layer boundaries
 * (request → use case, use case → response).
 * They are plain TypeScript interfaces/types — no runtime code.
 */

// export type { RegisterUserDto, LoginUserDto } from './auth.dto';
// export type { CreateCarDto, UpdateCarDto }     from './car.dto';
// export type { CreateBrandDto }                 from './brand.dto';
// export type { CreateCategoryDto }              from './category.dto';
