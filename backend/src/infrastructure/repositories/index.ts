/**
 * Prisma repository implementation barrel export.
 *
 * Concrete implementations of the domain repository interfaces using Prisma.
 * These classes are the ONLY place PrismaClient is used for data access.
 *
 * Stale references to PrismaCarRepository, PrismaBrandRepository, and
 * PrismaCategoryRepository have been removed — those models no longer exist
 * in the schema. New implementations are exported here as they are built.
 */

// export { PrismaUserRepository }    from './PrismaUserRepository';
// export { PrismaVehicleRepository } from './PrismaVehicleRepository';
