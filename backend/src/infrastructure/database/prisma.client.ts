/**
 * Prisma client stub.
 * Note: DriveVault uses MongoDB with Mongoose (mongodb.client.ts) for production persistence.
 * This stub prevents build failures when @prisma/client is not installed.
 */

export const prisma = null;
export default prisma;
